CAMERA CONFIG FILE v1

capitalised words / phrases are keywords the reader searches for
spaces are skipped
; indicates end of line (data entry)
on a keyword line comments can go after;

START_OF_DATA;
VERSION = 1;

PV_COMMANDS_START;              These are Pvs that are needed to send commands, i.e. switch things on / off, They are not monitored.

#pv_suffix must come first before other paramaters to be added to the struct.

PV_SUFFIX_STOP 			= StopAHA;
PV_CHTYPE      			= DBR_ENUM;

PV_SUFFIX_DATA			= CAM:ArrayData;
PV_CHTYPE      			= DBR_TIME_LONG;
PV_MASK        			= DBE_VALUE;

PV_SUFFIX_BKGRND_DATA	= CAM:BackgroundArrayData;
PV_CHTYPE      			= DBR_TIME_LONG;
PV_MASK        			= DBE_VALUE;

PV_SUFFIX_FILE_PATH 			= BIN:FilePath;
PV_CHTYPE      			= DBR_STRING;

PV_SUFFIX_FILE_NAME 			= BIN:FileName;
PV_CHTYPE      			= DBR_STRING;

CAM_FILE_PATH 			= BIN:FileTemplate;
PV_CHTYPE      			= DBR_STRING;

PV_SUFFIX_FILE_WRITE 			= BIN:FileWriteMode;
PV_CHTYPE      			= DBR_ENUM;



PV_MONITORS_START;  These are the PVs that will be continuously monitored !

PV_SUFFIX_ACQUIRE 		= CAM:Acquire;
PV_CHTYPE       		= DBR_ENUM;

PV_SUFFIX_CAM_STATE 			= CAM:ReadStatus;
PV_CHTYPE      			= DBR_ENUM;
PV_COUNT            = 1;
PV_CHTYPE           = DBR_ENUM;
PV_MASK             = DBE_VALUE;

OBJECTS_START;

NUMBER_OF_OBJECTS = 2; For sanity checks
NUMBER_OF_ILOCKS  = 0;  If the number of interlocks changes between objects, then redefine this before the "name" paramater


NAME    = VC;
PV_ROOT = CL-EA-DET-01:;
SCREEN_PV  = YAG01;
BIT_DEPTH = 16;
IMAGE_WIDTH = 2560;
IMAGE_HEIGHT = 2160;

NAME    = CAM01;
PV_ROOT = CL-EA-DET-01:;
SCREEN_PV  = YAG02;
BIT_DEPTH = 12;
IMAGE_WIDTH = 2560;
IMAGE_HEIGHT = 2160;


END_OF_DATA;
